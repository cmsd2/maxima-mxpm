/* {{ name }} — TODO: describe this package
 *
 * Usage:
 *   load("{{ name }}");
 *   {{ mac_name }}_hello();
 */

/* Uncomment after running 'mxpm doc build' to enable ? and ?? help lookup */
/* load("{{ name }}-index.lisp")$ */

{{ mac_name }}_hello() := print("{{ name }} is loaded!")$
